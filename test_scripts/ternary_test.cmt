import 'unittest' as unittest

function test_positive_ternary() {
    var a = true ? 'pass' : 'fail'
    unittest.Assert.that(a).is_equal_to('pass')
}

function test_negative_ternary() {
    var b = false ? 'fail' : 'pass'
    unittest.Assert.that(b).is_equal_to('pass')
}